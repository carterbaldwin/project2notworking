<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg}}</h1>
        <h2>This is a bar!</h2>
        <form class="clearfix mb-4" action="POST">
          {{!-- THIS IS THE BROKEN THING --}}
          {{!-- <div class="form-group">
            <label for="example-text">Comment Text</label>
            <input type="text" id="example-text" class="form-control" aria-describedby="example-text" placeholder="Enter Example Text">
          </div> --}}
          <div class="form-group">
            <label for="example-description">Comment Text</label>
            <textarea class="form-control" id="example-description" aria-describedby="example-description" placeholder="Enter Comment"></textarea>
          </div>
          <button id="submit" class="btn btn-primary float-right">Submit</button>
        </form>
        <h3>Comments:</h3>
        <ul id="example-list" class="list-group">
          {{#each examples}}
          <li data-id="{{this.id}}" class="list-group-item">
            <html="example/{{this.id}}">{{this.description}}</html>
            {{!-- <button class="btn btn-danger float-right delete">ｘ</button> --}}
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
